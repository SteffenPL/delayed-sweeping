[project]
name = "delayed-sweeping-plotting"
version = "0.1.0"
description = "Plotting tools for delayed sweeping simulations"
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.7.0",
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "tomli>=2.0.0; python_version < '3.11'",
    "toml>=0.10.2",
]

[tool.uv]
package = false
